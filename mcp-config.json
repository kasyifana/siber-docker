{
  "mcpServers": {
    "security-audit": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "mcp-security-server",
        "python",
        "-m",
        "src.server"
      ],
      "description": "Security audit server running in Docker container",
      "note": "This connects to the Docker container named 'mcp-security-server'",
      "prerequisites": [
        "Docker must be running",
        "Run 'docker-compose up -d' first to start the container"
      ],
      "features": [
        "port-scanning",
        "sql-injection-testing",
        "xss-testing",
        "ssl-analysis",
        "header-analysis",
        "subdomain-enumeration",
        "web-vulnerability-scanning"
      ]
    }
  }
}
